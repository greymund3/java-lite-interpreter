# java-lite-interpreter

 Java Interpreter is a Racket-based interpreter for a simplified Java-like language. It implements core programming language features such as variable declarations, assignment, lexical scoping, conditionals, loops, functions, return statements, break/continue control flow, and structured exception handling with try/catch.

The project focuses on language evaluation, environment management, and continuation-based execution to model complex control flow in a functional programming style.
